import React, {
	Component,
	useState,
	useEffect
} from 'react';
import styles from './index.module.scss';

export default function Page({
	projectId
}) {
	return (
		<div>
      		获客分析
    	</div>
	);
}